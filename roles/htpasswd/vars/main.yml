---
htpasswd_dir: "{{ lookup('ENV', 'HOME') }}/htpasswd"
htpasswd_file_path: "{{ lookup('ENV', 'HOME') }}/htpasswd/htpasswd"
htpasswd_cr_path: "{{ lookup('ENV', 'HOME') }}/htpasswd/htpasswd-cr.yaml"

htpasswd_credentials:
  - name: admin
    password: redhat
  - name: user1
    password: redhat
  - name: user2
    password: redhat
